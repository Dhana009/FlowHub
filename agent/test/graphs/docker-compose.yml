version: "3.9"
services:
  neo4j:
    image: neo4j:5.17
    container_name: neo4j-local
    ports:
      - "7474:7474"  # HTTP Browser
      - "7687:7687"  # Bolt protocol
    environment:
      - NEO4J_AUTH=neo4j/password123
    volumes:
      - neo4j_data:/data
volumes:
  neo4j_data:
